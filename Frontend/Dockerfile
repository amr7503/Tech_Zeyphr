/*
	Dockerfile disabled in repository to avoid Render autodetection for the Frontend static service.
	A copy of the original Dockerfile was saved as Dockerfile.disabled in the same folder.
	If you need to re-enable Docker-based deployment, rename Dockerfile.disabled -> Dockerfile
*/
